{
  "name": "lifehack2021",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd ./server && npm install && npm start",
    "start_frontend": "cd ./client && npm start",
    "client": "cd ./client && npm install && npm run build",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm run client"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react-css-blur": "^1.1.1"
  }
}
