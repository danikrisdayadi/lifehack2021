{
  "name": "lifehack2021",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./backend/server && npm run start_frontend",
    "start_frontend": "cd ./client && npm run start_client",
    "client": "cd ./client && npm install && npm run build",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install && npm run client"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react-css-blur": "^1.1.1"
  }
}
